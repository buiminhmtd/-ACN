@model IEnumerable<MovieBooking.Models.Booking_Details>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmins.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Booking.booking_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Seat.seat_row)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Booking.booking_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seat.seat_row)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.booking_detail_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.booking_detail_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.booking_detail_id })
        </td>
    </tr>
}

</table>
