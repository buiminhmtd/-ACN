@model IEnumerable<MovieBooking.Models.Seat>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmins.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Screen.screen_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.seat_row)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.seat_number)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Screen.screen_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.seat_row)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.seat_number)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.seat_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.seat_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.seat_id })
        </td>
    </tr>
}

</table>
